dist/
__pycache__/
*.pyc
.DS_Store

# Secrets — never commit
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# Drafts — stay local until ready to publish
posts/drafts/

# Editorial / strategy docs — keep local, don't expose on public repo
CLAUDE.md

# Scraped Wix product catalogue + assignment maps — derived, regenerate as needed
product_catalogue.json
*_assignments.json

# Claude Code workspace — local tooling, memory, slash commands
.claude/

# Editor / OS noise
.vscode/
.idea/
*.swp
Thumbs.db
